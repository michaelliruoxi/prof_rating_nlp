# --- Your original constraints ---

numpy<2
ml-dtypes<0.5
opencv-python-headless<4.9
pandas<2.3
scipy<1.12
scikit-learn<1.5
matplotlib
seaborn
jupyter

# --- Needed for keras imports (Keras is inside TF, but tf-keras depends on these) ---

h5py
protobuf
absl-py
gast
opt-einsum

# --- Needed for urllib (Python stdlib, no need to install) ---

# --- Needed for environment & utilities ---

six
tqdm

# --- Needed for image processing & visualization ---

Pillow

# --- Additional requirements for this NLP project ---

# Natural Language Processing
nltk

# Imbalanced learning (for random oversampling and other balancing techniques)
imbalanced-learn

# Gradient boosting (for Model 4 - XGBoost)
xgboost

# Deep learning framework (for Model 6 - Neural Network with LSTM)
tensorflow

# Note: NLTK data (punkt, punkt_tab, wordnet, omw-1.4) should be downloaded
# via nltk.download() in the notebook, not installed via pip

